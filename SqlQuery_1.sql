CREATE TABLE news (
  id INT(11) NOT NULL AUTO_INCREMENT,
  title VARCHAR(255) NOT NULL,
  description TEXT NOT NULL,
  date DATE NOT NULL,
  PRIMARY KEY (id)
  INSERT INTO news (title, description, date)
VALUES ('Nouvelle actualité', 'Description de la nouvelle actualité', '2023-03-23');

);
